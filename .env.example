# MySQL Binlog Configuration
BINLOG_HOST=localhost
BINLOG_PORT=3306
BINLOG_USERNAME=root
BINLOG_PASSWORD=changeme

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Docker Compose
MYSQL_ROOT_PASSWORD=changeme
