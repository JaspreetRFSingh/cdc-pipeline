spring.application.name=cdc-pipeline
server.port=8080
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
cdc.binlog.host=${BINLOG_HOST:localhost}
cdc.binlog.port=${BINLOG_PORT:3306}
cdc.binlog.username=${BINLOG_USERNAME:root}
cdc.binlog.password=${BINLOG_PASSWORD:changeme}
